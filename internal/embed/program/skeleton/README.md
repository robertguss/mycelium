# Idea name

One-line purpose stub.

Open this folder in your agent. State the goal for this sitting
(capture the spark, work a question, park it, wake it, hand it off).
The agent should follow `.agents/skills/session/SKILL.md`.

Run `mycelium check` from this directory to verify convention conformance.
You commit. The CLI never does.
